import React from "react";
import "./spinner.css"; // Or the path to where you placed the .spinner-fade class

const Spinner = () => {
    return (
<div className="loadingtext">
  <p>Loading</p>
</div>

    );
};

export default Spinner;
